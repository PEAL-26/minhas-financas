import { ContainerBody } from './container-body';
import { ContainerHeader } from './container-header';
import { ContainerRoot } from './container-root';

export const Container = {
  Root: ContainerRoot,
  Body: ContainerBody,
  Header: ContainerHeader,
};

export const Root = ContainerRoot;
export const Body = ContainerBody;
export const Header = ContainerHeader;
